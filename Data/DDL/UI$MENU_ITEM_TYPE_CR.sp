CREATE OR ALTER PROCEDURE UI$MENU_ITEM_TYPE_CR (
    I_ID VARCHAR(30) = NULL)
RETURNS (
    ID VARCHAR(30),
    NAME VARCHAR(100))
AS
BEGIN
  FOR SELECT
    ID,NAME
    FROM UI$MENU_ITEM_TYPE T 
    WHERE
      (T.ID = :I_ID OR :I_ID IS NULL) AND
      1=1
    INTO :ID,:NAME 
  DO
    SUSPEND;
END